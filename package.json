{
  "name": "launchtest",
  "version": "1.0.0",
  "description": "Minimal iOS app to verify IPA signing & launch",
  "scripts": {
    "build": "echo 'static'"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  }
}
